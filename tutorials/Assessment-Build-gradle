Use  cohortIdentifier = "cnd-batch2-pdc" in assignment-submission app in build.gradle

buildscript {
    repositories {
        maven { url "http://platform-acceleration-lab-maven.s3.amazonaws.com" }
        jcenter()
    }

    dependencies {
        classpath "io.pivotal.pal.assignments:assignments-plugin:4.1.0"
    }
}

apply plugin: "io.pivotal.pal.assignments.plugin"

assignments {
    apiUrl = "https://wipro-waveland.education.pivotal.io/"
    emails = [
       "pramod.kumar54@wipro.com",
	   "ajay.sharma32@wipro.com",
    ]
    cohortIdentifier = "cnd-batch2-pdc"
}

defaultTasks "tasks"