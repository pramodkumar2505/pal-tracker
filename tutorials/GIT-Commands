Steps to Sync local git repo to github
1) git init  //creates local git repo
2) git status //show local git repo status
3) git add . //changes adding to local repo its going to add in local .git repo after that again check git status
4) git commit -m "add ref comment"
5) git remote add origin http://github.com/pramodkumar2505/pal-tracker.git //mapping local git repo to github repo
6) git push origin master -f //Pushing all code stuff into github repo

git update-index --chmod=+x gradlew //It provides excute persmission for gradlew

git branch
git branches

git push origin master --tags

git push origin master username password git-url//to push

----------------------------------------------------------------
echo "# pal-tracker-distributed" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/pramodkumar2505/pal-tracker-distributed.git
git push -u origin master

